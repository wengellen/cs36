def csFindAllPathsFromAToB(graph):
	# we'll need a list to keep track of the current path
	# we'll need a list to keep track of the overall result

	# call to our recursive function to handle traversing through
	# our graph and building up a path, and then adding that path
	# to the overall result list

	# return our result list

	# our recursive function
	# base case?
	# once we've gone through every single graph node
	# we've built up the current path
	# append it to our result list
	# stop executing

	# loop through our current graph node's neighbors
	# append the neighbor to our path
	# recurse with the neighbor
	# pop that neighbor from our path